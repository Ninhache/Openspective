# Flag tier — soft / borderline. A match here (and no block/swear match) => "flag"
# (send to a human moderator rather than auto-rejecting). Same format as block.txt.
# Generic swears moved to swear.txt; only genuinely borderline insults remain.

# --- FR ---
tamere
